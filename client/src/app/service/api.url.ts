const URL_LOGIN = '/login';
const URL_USER = '/api/users';
const URL_PROJECT = '/api/projects';

export const LOGIN = {
  url: URL_LOGIN
};

export const USER = {
  url: URL_USER
};

export const PROJECT = {
  url: URL_PROJECT
};
